cval = 'a' + 3; // 'a'转换成int 然后'a'+3的结果转换成char
fval = ui - ival * 1.0; //ival,ui转换成double 然后相减的结果转换成float
dval = ui * fval; // ui转换成float 然后相乘结果转换成double
cval = ival + fval + dval;//ival和fval转换成double 然后3者相加的结果转换成char